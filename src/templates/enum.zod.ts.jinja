import { z } from 'zod';

export const {{ class_name }} = z.enum([
  {%- for (name, _) in members | sort %}
  '{{ name }}',
  {%- endfor %}
]);

export type {{ class_name }} = z.infer<typeof {{ class_name }}>;
